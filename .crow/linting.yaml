when:
  - event: push
    branch: main
  - event: pull_request
    branch: main

steps:
  prek:
    image: "cr.codefloe.com/ghcr.io/astral-sh/uv:python3.14-trixie"
    depends_on: []
    commands:
      - uv sync --locked --dev --all-extras
      - uv run prek run -a
