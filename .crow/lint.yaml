when:
  - event: push
    branch: main
  - event: pull_request
    branch: main

steps:
  pre-commit:
    image: "python:3.14"
    commands:
      - pip install --no-cache-dir "pre-commit==4.3.0"
      - pre-commit run -a
