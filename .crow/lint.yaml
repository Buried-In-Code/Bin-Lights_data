when:
  - event: push
    branch: main
  - event: pull_request
    branch: main

steps:
  pre-commit:
    image: "ghcr.io/astral-sh/uv:python3.14-trixie"
    commands:
      - uv sync --locked --group dev
      - uv run pre-commit run -a
